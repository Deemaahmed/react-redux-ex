import * as types from "./user.types"


//HINT: use here the variable you want to change
const initialState = {
}


export const userReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}